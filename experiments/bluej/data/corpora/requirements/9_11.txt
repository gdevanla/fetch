Specify libraries that are to be used in a single projectCreate a folder in the projectfolder and call it “+libs” (without the quotationmarks) and put the jar files in that folder.The classes made available by a library can also be use interactively with the“Use Library Class” function (See Create Objects from Available LibraryClasses) or the “Evaluate Expression” (See Evaluate a single expression)
